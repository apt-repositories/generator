name: QA

permissions:
  contents: read
  packages: read

on:
  pull_request:
  push:
    branches:
      - "**"
      - "!main"

jobs:
  qa:
    name: 🔹 QA
    uses: oliversalzburg/workflows/.github/workflows/qa-npm-package.yml@main
    permissions:
      contents: read
      packages: read
    with:
      has-mkdocs: false
